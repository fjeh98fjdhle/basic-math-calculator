def sum(self, other):
    a = float(self) + float(other)  
    return a

def sub(self, other): 
    a = float(self) - float(other)
    return a 

def mul(self, other): 
    a = float(self) * float(other)
    return a 

def div(self, other): 
    a = float(self) / float(other)
    return a 
